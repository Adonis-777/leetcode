class Solution {
public:
    void reverseString(vector<char>& s) {

        int n = s.size() / 2;
        int a = s.size();
        for(int i = 0; i < n; i++){
            char temp = s[i];
            s[i] = s[a - i - 1];
            s[a - i - 1] = temp;
        }
        
    }
};
